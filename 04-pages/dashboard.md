# 工作台规范
## Dashboard Standard

Keywords: dashboard, workspace, metrics, overview, backend, ai readable, html preview, vue codegen

> 本文件是 `04-pages/` 页面规范的一部分，用于约束 AI 生成后台页面、HTML 预览和 Vue 3 + TypeScript + Ant Design Vue 页面代码。
>
> 全局框架、导航、页面容器、颜色、间距、圆角、投影、组件尺寸等通用规则，应优先遵循：
>
> ```text
> 03-interaction/platform-frame.md
> 03-interaction/page-container.md
> 01-foundation/layout.md
> 01-foundation/tokens.md
> 01-foundation/shadow.md
> 02-components/component-size.md
> ```
>
> 本文件只补充当前页面类型的结构、模块、状态、交互与生成要求。

## Dashboard / 工作台页面模板规范

Dashboard / 工作台用于承载企业级系统的整体态势、核心指标、趋势分析、风险告警、任务待办和明细入口。页面应遵循“先概览、再诊断、后行动”的阅读路径，优先保证数据可信、层级清晰和操作明确。

### 通用页面骨架

```text
顶部全局导航
左侧一级 / 二级导航
页面标题区 / 面包屑 / 时间筛选 / 刷新操作
核心概览区
风险告警 / 状态摘要区
图表分析区
排行 / 分布 / 趋势区
明细表格 / 任务列表 / 事件列表
分页 / 辅助操作区
```

| 区域 | 规范 |
|---|---|
| 顶部导航 | 固定在页面顶部，深色背景优先，承载产品 Logo、一级导航、全局状态、通知、用户入口 |
| 左侧导航 | 白色或浅灰背景，当前项使用主色竖条与浅绿色选中底色，支持展开 / 收起 / 二级菜单 |
| 内容背景 | 使用浅灰背景，卡片使用白色底，模块之间通过 16px / 24px 间距区分 |
| 页面标题区 | 标题左对齐，时间筛选、刷新、搜索等操作右对齐 |
| 卡片容器 | 白底、弱边框或轻投影，圆角 4px / 6px / 8px，内边距 16px / 20px / 24px |
| 数据模块 | 核心指标优先放在首屏上方，图表放在中部，表格和明细放在下方 |
| 视觉重心 | 重要数值、风险、异常、AI 评分或中心图表作为首屏视觉焦点 |
| 滚动方式 | 单屏 Dashboard 使用 16:9；长页工作台可采用 1440px 宽度与自适应页面高度 |

### 顶部导航与侧边栏规范

#### 顶部导航

- 顶部导航高度建议 56px。
- 背景使用深色中性底，文字使用白色或浅灰。
- 一级导航横向排列，当前项使用主色下划线或主色高亮。
- 右侧放置系统运行状态、订阅信息、通知、帮助、用户身份与下拉入口。
- 顶部导航不承载过多业务筛选条件，业务筛选应放入内容区或二级标签区。

#### 侧边栏

- 展开态侧边栏建议 200px / 220px。
- 收起态侧边栏建议 56px。
- 当前菜单项使用浅绿色背景 + 主色竖条 + 主色文字。
- 二级菜单缩进显示，父级菜单使用展开 / 收起箭头。
- 菜单图标保持线性、轻量、统一描边。
- 页面底部可放置版本号、折叠菜单入口或系统辅助信息。

### 页面顶部工具区规范

页面顶部工具区用于承载当前页面标题、范围筛选和高频操作。

| 元素 | 规范 |
|---|---|
| 页面标题 | 4–12 字，字号 16px / 18px，字重 500 / 600 |
| 时间筛选 | 优先放在右侧，高度 32px，支持近 7 天、近 30 天、自定义时间范围 |
| 刷新按钮 | 位于时间筛选附近，使用次按钮样式 |
| 搜索框 | 可用于资产、IP、域名、事件、任务检索，高度 32px |
| 筛选器 | 单行不超过 4 个，复杂筛选放入展开面板 |
| 二级标签 | 用于切换二级模块、分类看板或数据维度，当前项使用主色下划线 |

### 核心指标卡片规范

核心指标卡用于快速呈现系统最重要的数量、比例、趋势和风险状态。

```text
图标 / 状态角标
指标标题
核心数值 + 单位
趋势箭头 / 同比环比 / 状态说明
```

| 项目 | 规范 |
|---|---|
| 单屏数量 | 4–8 个 |
| 卡片高度 | 80px / 96px / 120px |
| 图标容器 | 40px / 48px，浅色底，主色图标 |
| 指标标题 | 12px / 14px，中性色 |
| 核心数值 | 24px / 28px / 32px，深色，高字重 |
| 单位 | 放在数值右侧，字号低一级 |
| 趋势 | 上升使用绿色，下跌或风险使用红色 / 橙色 |
| 分隔方式 | 横向指标卡之间可使用竖向分割线或独立卡片间距 |

指标卡应避免堆叠长句。每张卡只表达一个核心指标。

### 风险告警与状态摘要规范

风险告警区用于突出异常、风险、告警、待处理事项和系统健康状态。

#### 风险告警条

```text
警告图标
风险摘要文案
关键风险数量
辅助指标
主操作按钮
```

- 风险告警条应靠近首屏上方。
- 高危风险使用红色或橙红色强调，背景使用浅红 / 浅橙色。
- 风险文案应包含数量和风险等级，例如“存在高危风险 33 分”。
- 主操作按钮用于“立即体检”“查看详情”“处理风险”等直接行动。
- 告警条不宜承载复杂表格，只表达核心风险与行动入口。

#### 状态摘要区

- 可采用三列结构：安全状态、风险资产、安全运维。
- 每列包含图标、指标名称、数量与单位。
- 状态类数据应使用成功、警告、错误、风险等级色区分。
- 事件类区域可使用标签页切换：安全事件 / 威胁告警 / 任务动态。

### 图表区布局规范

Dashboard 图表区应根据数据关系选择布局，不应为了填充页面而堆叠图表。

| 图表区类型 | 推荐布局 | 使用场景 |
|---|---|---|
| 两列图表区 | 1:1 或 2:1 | 趋势 + 占比、排名 + 分布 |
| 三列图表区 | 4:4:4 | 多个同级指标对比 |
| 主图 + 辅图 | 8:4 或 9:3 | 核心趋势 + 辅助分布 |
| 卡片网格 | 3 / 4 列等宽 | 监控状态、资源卡片、趋势小图 |
| 长页分析区 | 按主题纵向分区 | 安全运营、资源监控、资产盘点 |
| 全宽趋势区 | 12 栅格 | 时间跨度较长、数据点较多的趋势分析 |
| 全宽表格区 | 12 栅格 | 明细数据、事件列表、监控记录 |

### 图表类型组合规范

| 数据目标 | 推荐图表 | 展示规则 |
|---|---|---|
| 总体评分 | 仪表盘 / 环形进度 / 中心大数值 | 可作为首屏视觉中心 |
| 趋势变化 | 折线图 / 面积图 | 适合流量、风险、事件、使用率变化 |
| 类别占比 | 环形图 / 饼图 | 中心可放总数，右侧放图例 |
| Top 排名 | 横向条形图 | 用于漏洞 Top、病毒 Top、攻击来源 Top |
| 设备状态 | 进度条 / 横向排行 / 环形图 | 展示在线率、设备类型、容量占用 |
| 风险分布 | 环形图 / 分级柱状图 | 使用高 / 中 / 低风险色彩映射 |
| 能力覆盖 | 雷达图 | 用于防护能力、检测能力、技术维度对比 |
| 矩阵分析 | 热力矩阵 / 分组网格 | 用于策略覆盖、防护效果、攻击面分布 |
| 明细记录 | 表格 | 放在页面下方，支持排序、分页和详情入口 |

单屏图表类型不宜超过 3 种。长页 Dashboard 可按主题分区使用更多图表类型，但每个分区内部应保持视觉一致。

### Dashboard 页面类型模板

#### 综合态势工作台

用于展示企业、项目、系统或安全业务的整体运行状态。

```text
顶部导航
侧边栏
企业 / 项目概览横幅
关键指标总览
风险告警条
安全状态与事件摘要
资产盘点模块
运营看板模块
服务资源模块
```

必备模块：

- 企业 / 项目名称与授权状态。
- 3–5 个核心待办 / 待处理 / 待施工 / 待评估指标。
- 风险告警条，包含风险数量、风险等级和处理入口。
- 安全状态检测或健康度摘要。
- 安全事件 / 威胁告警 / 任务动态标签区。
- 资产盘点模块，包含终端统计、设备概况、设备类型分布。
- 运营分析模块，包含类型分布、Top 排名、趋势曲线。
- 服务资源模块，包含 CPU / 内存 / 磁盘 / 容量占用等资源指标。

生成约束：

- 首屏必须突出风险或待办，不要只展示普通图表。
- 长页内容应按“概览—风险—资产—运营—资源”纵向排列。
- 每个大分区必须有清晰标题。
- 图表和事件列表应保持 6:4 或 1:1 的视觉平衡。

#### 实时监控 Dashboard

用于展示系统、组件、设备或服务的实时状态。

```text
顶部导航
二级标签栏
刷新按钮
状态总览卡
组件拓扑 / 状态关系图
实时趋势图
容量与排行卡片
监控明细表格
```

必备模块：

- 总体状态卡，展示运行时长、异常状态或健康度。
- 组件状态图，中心为核心组件，周围为正常 / 异常节点。
- 最近 5 分钟或最近 10 分钟趋势图。
- 容量卡片，展示已用容量、剩余容量、磁盘列表。
- Top 排名卡片，展示数据源、设备、任务或资源排行。
- 底部监控表格，展示名称、类型、状态、分片、目录、更新时间等字段。

生成约束：

- 卡片网格优先采用 3 列或 4 列布局。
- 实时监控类图表应显示时间轴与单位。
- 正常节点使用绿色，异常节点使用红色，告警节点使用橙色。
- 底部明细表格应横跨全宽。

#### AI 决策分析 Dashboard

用于展示 AI 检测、风险判断、决策评分和策略建议。

```text
顶部导航
页面标题与时间范围
AI 决策评分英雄区
检测能力指标
风险总数与风险等级
AI 决策建议
趋势柱状图
指标摘要卡
分析图表区
分项分析标签页
```

必备模块：

- 中央大号评分，例如 80 分、92 分、A级等。
- 评分周围展示检测项、识别项、风险项、工具项等能力指标。
- 左侧展示流量、峰值、处理量等基础指标。
- 右侧展示事件总数、高风险、中风险、低风险数量。
- 中部展示 AI 分析建议或决策结论。
- 下方展示柱状图、分项趋势、风险分布和明细标签页。

生成约束：

- AI 评分区可以有轻科技感，但不能过度炫光。
- 评分必须有明确标题和业务含义。
- AI 建议区必须体现“可行动建议”，不能只写空泛描述。
- AI 标签和评分色彩应保持克制。

#### 安全防护能力 Dashboard

用于展示安全能力覆盖、策略命中、攻击防护和检测结果。

```text
顶部导航
安全评分 / 防护等级
能力覆盖卡片
攻击趋势
风险等级分布
防护能力矩阵
事件明细表格
```

必备模块：

- 安全评分或整体防护等级。
- 能力覆盖率，如终端覆盖、策略覆盖、日志覆盖。
- 攻击趋势或风险趋势。
- 高 / 中 / 低风险分布。
- 防护能力矩阵或雷达图。
- 风险事件或告警列表。

生成约束：

- 高风险信息应优先展示在首屏。
- 风险颜色需符合语义，不可随意使用高饱和多色。
- 表格中必须包含风险等级、状态和操作入口。

#### 运营分析 Dashboard

用于展示业务运营、使用情况、资源消耗和趋势分析。

```text
关键指标卡
趋势分析区
分类占比区
Top 排名
运营明细表格
```

必备模块：

- 访问量、任务量、处理量、成功率、失败率等指标。
- 时间趋势图。
- 分类分布图。
- Top 排名。
- 明细记录表。

生成约束：

- 运营分析不应使用过多风险色。
- 趋势图应突出变化方向。
- 指标卡需包含同比 / 环比或状态说明。

### 明细表格与任务列表规范

Dashboard 底部可放置明细表格、任务列表、事件列表或动态列表。

表格常见字段：

- 名称 / 对象
- 类型 / 分类
- 状态 / 风险等级
- 时间
- 来源
- 负责人
- 操作

规则：

- 明细区应位于图表之后。
- 表格不应抢占核心指标首屏。
- 操作列保持在最右侧。
- Dashboard 中表格行数建议 5–8 行。
- 查看更多入口应明确。

### Dashboard AI 生图要求

生成 Dashboard 图片时，必须满足：

- 16:9 或标准后台页面比例。
- 顶部导航、左侧导航、内容区结构清楚。
- 核心指标、风险告警、图表和列表层级分明。
- 图表数量克制，单屏不超过 3–5 个主要图表。
- 不要把 Dashboard 生成纯数据大屏，除非用户明确要求大屏。
- 不要使用过度 3D、炫光、复杂地图和无意义装饰。
- 所有主要中文标题应可读。
- 安全类页面可使用绿色、蓝色、橙红色作为语义点缀。
- AI 类页面可使用轻渐变，但不可破坏后台系统的清晰度。

### Vue / HTML 生成要求

生成 Dashboard 代码时，必须包含：

- 页面标题区。
- 核心指标数据模型。
- 风险告警或状态摘要。
- 至少 2 个图表区域或模拟图表区域。
- 明细表格或任务列表。
- loading 状态。
- empty 状态。
- error 状态。
- mock 数据。
- 基础点击交互，例如刷新、时间范围切换、卡片 hover、查看详情。

HTML 预览文件应支持：

- 时间范围切换。
- 刷新按钮反馈。
- 卡片 hover。
- 标签切换或图表维度切换。
- 查看更多 / 查看详情入口。

### Dashboard 验收检查清单

| 检查项 | 要求 |
|---|---|
| 页面结构 | 顶部导航、侧边栏、标题区、指标、图表、明细完整 |
| 阅读路径 | 符合“概览—诊断—行动”顺序 |
| 指标卡 | 数量克制，每张卡只表达一个核心指标 |
| 风险提示 | 高风险或待办有明显入口和操作按钮 |
| 图表组合 | 图表类型不超过 3 种，布局平衡 |
| 表格明细 | 放在下方，字段与操作完整 |
| 交互状态 | loading、empty、error、刷新、筛选状态齐全 |
| 视觉克制 | 不做成营销页或纯炫酷大屏 |
