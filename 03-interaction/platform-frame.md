# 平台固定框架规范
## Platform Frame Rules

Keywords: platform frame, html template, top navigation, sidebar, page header, content slot, backend

本文定义后台页面固定框架的继承规则。平台固定框架不是视觉参考稿，也不是重新实现框架的文字蓝图。生成 HTML Demo 时，必须继承 `06-vue-code/templates/` 下的对应 HTML 母版，不得根据本文描述重新实现同名框架结构。

## 1. 最高优先级

所有后台 HTML Demo 必须先确定并继承固定 HTML 母版，再生成业务内容。

默认母版：

```text
06-vue-code/templates/common-single-nav.html
```

只有用户明确指定双层导航时，才使用：

```text
06-vue-code/templates/double-nav-frame.html
```

## 2. 框架组成

固定框架由以下部分组成：

```text
Top Navigation
→ Sidebar
→ Page Header
→ Page Content / Business Content Area
```

业务页面只能替换业务内容区，不得重写顶部导航、侧边导航、母版页头或外层内容容器。

## 3. 必须保留

HTML Demo 必须完整保留母版中的：

- 顶部导航 DOM、CSS、JS。
- 左侧菜单 DOM、CSS、JS。
- 页面标题区结构。
- 内容容器结构。
- iconfont。
- hover、active、open、collapsed 状态。
- 收起态级联浮层。
- 响应式规则。

## 4. 允许替换

只允许按业务替换：

- Logo 和系统名称。
- 顶部导航文案。
- 左侧菜单文案和选中项。
- 页面标题。
- 业务内容区内容。
- mock 数据。
- 与业务内容绑定的局部交互。

## 5. 顶部导航规则

- 高度固定，位置稳定。
- 左侧展示品牌和系统名称。
- 中间可展示运行状态或安全状态。
- 右侧展示功能入口、通知、设置、安全入口和用户信息。
- 不得因业务内容变化挤压或变形。

## 6. 侧边导航规则

- 展开态展示多级菜单文本。
- 收起态展示图标和级联浮层。
- 当前菜单高亮。
- 有子级菜单时支持展开 / 收起。
- 收起态级联浮层来自同一份菜单数据，不得把多级菜单堆在一个浮层里。
- 底部可保留版本号、收起按钮或辅助入口。

## 7. 页面标题区规则

- 页面标题区属于母版固定结构。
- 业务内容区内不得重新生成面包屑或额外顶层页头。
- 详情页中的对象名称、状态标签和业务操作应作为内容区内的摘要模块出现，不替代母版页头。

## 8. 业务内容区规则

业务内容区只承载当前页面业务模块，例如：

- 列表页：搜索区、工具栏、表格、分页。
- 表单页：表单分组、字段、操作区。
- 详情页：对象摘要、信息分组、关联记录。
- 工作台：指标、待办、风险、快捷入口。

业务内容可以追加业务 class，但不得替换母版框架 class。

## 9. HTML Demo 生成要求

生成任何 HTML Demo 时必须：

- 先读取并完整复制对应 HTML 母版。
- 默认使用 `common-single-nav.html`。
- 明确双层导航时使用 `double-nav-frame.html`。
- 保留母版顶部导航、左侧菜单、页头和内容容器。
- 业务内容只写入母版指定业务内容区。
- 使用规范 iconfont，不使用字符图标临时代替。
- 不新增游离色值。
- 不简化框架交互状态。

## 10. 禁止项

- 禁止重新设计顶部导航。
- 禁止重新设计左侧菜单。
- 禁止只参考母版视觉但不复制母版 DOM / CSS / JS。
- 禁止把业务页面写到母版外层。
- 禁止在业务内容区重新生成一套页面框架。
- 禁止删除收起态、hover、active、open、级联浮层等框架交互。

缺少任一固定框架关键结构，视为 HTML Demo 生成失败。
